apiVersion: v1
kind: Service
metadata:
  name: blockchain-db-headless
  namespace: blockchain-network
spec:
  clusterIP: None
  selector:
    app: blockchain-db
  ports:
    - port: 5432
      targetPort: 5432

---
apiVersion: v1
kind: Service
metadata:
  name: blockchain-db
  namespace: blockchain-network
spec:
  type: ClusterIP
  selector:
    app: blockchain-db
  ports:
    - port: 5432
      targetPort: 5432
